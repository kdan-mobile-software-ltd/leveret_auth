# frozen_string_literal: true

class CreateDeviseAuthTables < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :identities do |t|
      t.string :uid, null: false
      t.string :provider, null: false
      t.references :owner,  null: false
      t.timestamps
    end

    add_index :identities, [:uid, :provider]

    # Uncomment below to ensure a valid reference to the owner's table
    # add_foreign_key :identities, <model>, column: :owner_id
  end
end